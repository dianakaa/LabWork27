<Page x:Class="LabWork27.Pages.RegistrationPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:LabWork27.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Регистрация">

    <Grid Background="AliceBlue">
        <Grid.ColumnDefinitions >
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Content="Логин"/>
        <Label Content="Пароль" Grid.Row="1"/>
        <Label Content="Подтверждение пароля" Grid.Row="2"/>
        <TextBox x:Name="LoginTextBox" Grid.Column="1"/>
        <TextBox x:Name="PasswordTextBox" Grid.Column="1" Grid.Row="1"/>
        <TextBox x:Name="PasswordConfirmation" Grid.Column="1"  Grid.Row="2"/>

        <StackPanel Orientation="Horizontal" Grid.Row="3">
            <Button x:Name="Ok" Content="Ок" Width="140" Click="Ok_Click" />
        </StackPanel>
    </Grid>
</Page>
